export class User {
    username: String;
    password: String;
  
    // constructor(user?: any) {
    //   this.username = user && user.firstName || null;
    //   this.password = user && user.lastName || null;
    // }
  }